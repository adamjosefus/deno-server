{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deno run",
            "type": "shell",
            "command": "deno run --import-map=import_map.json ./mod.ts",
            "group": "build",
            "problemMatcher": "$deno",
            "options": {
                "cwd": "${cwd}",
            }
        }
    ]
}